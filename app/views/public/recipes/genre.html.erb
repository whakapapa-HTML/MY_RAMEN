<div class="container">
  <div class="row">
    <% @recipes.each do |recipe| %>
    <div class="col-3 p-0 ">
      <%= link_to image_tag(recipe.recipe_image.url, class: "mt-5", size: '273x160'), recipe_path(recipe.id) %></br>
      <%= recipe.name %>
    </div>
    <% end %>
  </div>
</div>