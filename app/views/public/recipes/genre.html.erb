<div class="container">
  <div class="row">
    <% @recipes.each do |recipe| %>
    <div class="col-6">
      <%= link_to image_tag(recipe.recipe_image.url, class: "mt-5", size: '320x220'), recipe_path(recipe.id) %>
      <%= recipe.name %>
    </div>
    <% end %>
  </div>
</div>